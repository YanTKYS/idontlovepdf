# idontlovepdf

自治体内で一般職員が利用することを想定した「PDF編集ツール」。

PDFの処理は、同一開発者による別リポジトリ `YanTKYS/idontlovepdf-engine` が担当する。

| リポジトリ | 責務 |
|---|---|
| `idontlovepdf`（本リポジトリ） | 一般職員向けPDF編集ツールのUI / UX |
| `idontlovepdf-engine` | PDF解析・暗号化認証／復号・本文抽出・検索・置換・フォント埋め込み・保存 |

## 現在の段階

**試作版。PDFの文字を検索し、1件ずつ置き換え、変更前と編集中のPDFを見比べて、編集済みPDFとして保存できる。元のPDFのフォントに含まれていない日本語の文字へも置き換えられる。**

`idontlovepdf-engine` v0.4.3 の正式Release bundleと、編集用フォント BIZ UDGothic を同梱し、次の操作を一般職員向けの画面として提供する。

1. PDFを選択する
2. 直したい文字を検索する
3. 検索結果から1件を選ぶ
4. 新しい文字を入力する
5. 置換する
6. 変更前／編集中のPDFプレビューで結果を確かめる
7. 編集済みPDFを保存する

利用者がPDFの内部構造を理解する必要はない。画面にはPDF内部の情報（object番号、テキストの分割単位、フォント名等）を出さない。編集用フォントも利用者が意識する設定ではないため、フォントを選ぶ画面は設けていない。

確認できている範囲は「動作確認」の項に記載する。GitHub PagesとIISでの動作は未確認である。

## 判定

技術的実現性と運用可否は分けて扱う。

| 区分 | 判定 | 根拠 |
|---|---|---|
| 技術的実現性 | **あり** | 実PDFでの本文抽出・日本語ToUnicode・暗号化PDF・複数の描画単位にまたがる語句の検索・置換／保存に加え、元PDFのフォントに無い文字への置換（編集用フォントの埋め込み）まで確認済み |
| 運用可否 | **未判定** | 技術的な適用範囲は大きく広がったが、庁内IIS上での実業務PDFによる試行を継続して判断する |

検討の経緯と根拠は `docs/feasibility.md` に記録している。

## できること / できないこと

現時点でできること。

- PDFファイルを選択する（ボタン、またはドラッグ＆ドロップ）
- ブラウザ内でPDFを読み込み、本文テキストを認識する
- 本文の文字を検索する（部分一致）。PDF内部で複数の描画単位に分かれて記録されている語句も、続いた1つの文字列として検索できる
- 検索結果から1件を選んで置き換える
- **元のPDFのフォントに含まれていない日本語の文字へ置き換える**（同梱した編集用フォントで書く）
- 文字数が変わる置き換えを行う（PDFの作りによっては断られる。下記参照）
- 文字を削除する（置き換える文字を空欄にする）
- 変更前のPDFと編集中のPDFをその場でプレビューし、切り替えて見比べる
- 成功した置換の内容を、変更履歴として画面で確認する
- 編集済みPDFを `元の名前.edited.pdf` として保存する
- パスワード保護されたPDFで、パスワードを入力して開く
- エラーの内容を区分して表示する（パスワード、非対応形式、破損、使用できない文字、安全に置換できない箇所 等）

現時点でできないこと。

- 任意のPDFの任意の箇所を自由に編集すること（下の「置き換えられない場合」を参照）
- 「すべて置換」
- PDF上の文字を直接クリックしての編集、文字位置やフォントの変更
- 段落の折り返し直し（Wordのような再レイアウト）
- ページ回転・ページ挿入・ページ削除
- スキャンPDF（画像だけのPDF）の編集

### 置き換えられない場合

置換できるかどうかは、engineが対象の箇所ごとに判断する。本ツール側で文字数やフォントから可否を推測することはしない。**検索結果を選んでいれば、置換文字数にかかわらず「置換」を押せる。** 押した時点でengineが確認し、安全に書き換えられない場合だけ理由を区分して案内する。

| 場合 | 画面の案内 |
|---|---|
| 置換後の文字が、編集用フォントにも無い（外字・異体字等） | 置換できません（使用できない文字を表示） |
| PDFの作り（文字の並べ方・間隔の指定・縦書き等）により、書き換えると文字の配置が崩れる | この箇所は置き換えられません |
| パスワード等で暗号化されたPDF | 暗号化PDFの保存には現在対応していません |
| 文書側で変更が禁止されているPDF（`/P`） | 文書側で変更が制限されています |

いずれの場合も、別の箇所を選ぶか、元のファイル（Word等）からの修正を検討する。

安全に置き換えられない箇所を断るのは、engineの設計（fail closed）である。ずれたPDF・崩れたPDFを作らないことを優先しており、本ツール側でこれを迂回する処理は実装していない。**「同じ文字数なら必ず置き換えられる」わけではない。**

### 編集用フォントについて

元のPDFへ埋め込まれたフォントは、その文書で実際に使った文字だけを含むことが多い。そのため「元のPDFに出てこない文字」へ置き換えるには、別のフォントが必要になる。本ツールは BIZ UDGothic（SIL Open Font License 1.1）を同梱し、これを編集用フォントとして使う。

- 元のフォントで書ける場合は、従来どおり元のフォントで書く。編集用フォントは必要なときだけ使われる。
- 元のPDFのフォントに無い文字への置換は、engineが後続の文字位置を安全に維持できると判断した箇所で行える。engineは後続文字の位置を保つために元のフォントの文字幅を必要とし、v0.4.2 ではPDF自身が書いている幅情報を正確に読み取れる範囲が広がった。v0.4.3 では、その幅情報を持つ `/DescendantFonts` がinline dictionaryとして書かれている場合にも対応した。読み取れないフォントでは引き続き断る。どの箇所が該当するかを利用者が意識する必要はない。
- 編集用フォントが実際に使われた場合、そのフォントが保存するPDFへ埋め込まれるため、ファイルサイズが約3MB増える。同じPDFで置換を繰り返しても、増えるのは1回だけである。
- 編集用フォントはリポジトリに同梱したファイルだけを使う。実行時にインターネットから取得しない。
- 編集用フォントを読み込めない場合、ツールはエラーを表示してPDFを受け付けない。黙って機能を落とした状態では動かさない。確認する時点は次の2段階である。
  - **起動時**: ファイルが無い、サーバーが配信できない、中身が空 → その場でエラーになる
  - **最初のPDFを開いた時点**: 取得はできたが、フォントとして読めない（配信設定によりHTML等が返る、ファイルが壊れている等） → 編集画面を開かずエラーになる

## 構成

```text
index.html                          ツール本体
css/style.css                       画面のスタイル
js/app.js                           画面とエンジンの接続
vendor/idontlovepdf-engine.js       idontlovepdf-engine v0.4.3 の正式Release bundle
vendor/fonts/BIZUDGothic-Regular.ttf 編集用フォント（BIZ UDGothic Regular 1.05）
vendor/fonts/OFL.txt                編集用フォントのライセンス（SIL OFL 1.1）
vendor/README.md                    取り込んだengine・フォントの情報と更新手順
docs/feasibility.md                 実現可能性の検討記録
reference/guide_context.md          開発ガイド `lg_toolkit_guide` の最小ガイド（参照用）
.nojekyll                           GitHub PagesでJekyll処理を無効化する（後述）
```

npm、Node.js、esbuild、webpack、Vite、Parcel等のビルド環境は本リポジトリへ導入していない。service worker、WebAssembly、サーバー側処理も導入していない。

`idontlovepdf-engine` 側で生成済みのブラウザ向け1ファイルbundleを、そのままES Moduleとして読み込む。最終構成は静的ファイルのみである。

### エンジンとの責務分担

検索と置換の対象・可否を決める処理は、すべてengineの公開APIへ任せている。

| 処理 | 担当 |
|---|---|
| PDFの解析、テキストの分割単位の判断、語句の検索（`searchText()`） | engine |
| 置換可否の判断（`checkTextMatchReplacement()`）、置換（`replaceTextMatch()`）、保存（`save()`） | engine |
| 元のフォントで書くか編集用フォントで書くかの判断、フォントの埋め込み | engine |
| 画面表示、操作の順序、編集用フォントの読み込み、プレビュー、変更履歴、エラーの言い換え | 本リポジトリ |

本リポジトリでは、PDFの描画命令の解釈、テキストの分割単位の連結、フォントの中身の判定、object番号の参照といったPDF内部構造の判断を行わない。

## 外部依存・通信

- 実行時に外部通信は発生しない。
- 外部CDN、外部フォント配信、外部API、ライセンスサーバー、analyticsのいずれも利用しない。
- 編集用フォントはリポジトリ同梱のローカルファイルを、同一originの相対URLで読み込む。
- PDFプレビューはブラウザ標準のPDF表示機能とBlob URLだけで行う。PDF.js等の追加ライブラリも、外部のPDFビューアサービスも利用しない。
- エンジンbundleはリポジトリへ同梱している。実行時にGitHub Releaseから取得する方式は採らない。
- 選択したPDF、入力した検索文字・置換文字・パスワードは、いずれもブラウザ内だけで扱う。サーバーへ送らない。

## 個人情報・機密情報の取り扱い

- PDFの内容、検索文字、置換文字を送信・保存しない。
- 変更履歴は画面に表示するだけで、localStorage、sessionStorage、IndexedDB、cookieのいずれにも保存しない。PDFを選び直すと消える。
- 入力したパスワードは処理中のメモリ内だけで扱う。同じくどこにも保存しない。
- パスワードや暗号鍵を画面へ表示せず、ログにも出力しない。

## 利用環境

静的HTTPサーバーからの配信を前提とする。HTTPSは必須ではない。

- GitHub Pages
- IIS等の庁内静的Webサーバー（通常HTTP・閉域運用を含む）

配置は、リポジトリの内容をそのまま公開ディレクトリへ置くだけでよい。サーバー側の設定要件は次の2点である。

- `.js` をJavaScriptのMIME type（`text/javascript` 等）で配信すること（IISの既定設定で満たされる）
- `.ttf` を配信できること（IISの既定設定で満たされる）。MIME typeを絞った構成の場合のみ、`.ttf` → `font/ttf` の登録を確認する

`.ttf` を配信できないと編集用フォントを読み込めず、ツールは「編集用フォントを読み込めませんでした。配置を確認してください。」と表示して起動を止める。この表示が出た場合は、`vendor/fonts/BIZUDGothic-Regular.ttf` が公開ディレクトリへ配置されているかと、上記のMIME設定を確認する。

GitHub Pagesでは既定でJekyllが動作し、`vendor/` ディレクトリが公開対象から除外される。これを避けるため、リポジトリ直下に `.nojekyll` を置いている。**削除しないこと。**

PDFプレビューはブラウザ標準のPDF表示機能を利用する。この機能が無効な環境では、プレビュー欄に案内を表示し、検索・置換・保存はそのまま利用できる。

### `file://` での直接起動について

`index.html` を `file://` で直接開く方式は、正式な運用形態としない。

ES Moduleはブラウザのセキュリティ制約により `file://` から読み込めないためである。この場合は画面上に案内を表示する。

## 使い方

1. 静的HTTPサーバー経由で `index.html` を開く
2. 「PDFを選択」からPDFを選ぶ（ドラッグ＆ドロップも可）
3. 状態表示が「成功」になり、認識した本文の件数とPDFプレビューが表示される
4. 「検索する文字」に直したい文字を入れて「検索」を押す
5. 検索結果から直したい1件を選ぶ（前後の文章で見分ける）
6. 「置き換える文字」に新しい文字を入れて「置換」を押す
7. プレビューが「編集中」へ切り替わるので、直った内容を確認する
8. 「編集済みPDFを保存」を押してダウンロードする

補足。

- 置き換える文字を空欄にして置換すると、選択した文字を削除できる。
- 置換は1件ずつ行う。置換のたびに同じ検索文字で検索し直すので、残りの件数を確認しながら進められる。
- プレビューの「変更前」「編集中」は何度でも切り替えられる。「変更前」は最初に選んだPDFそのままである。
- 「変更 N件」は、編集中のPDFへ実際に反映された置換の件数である。失敗した置換や、同じ文字への置換は数えない。
- 元のPDFファイルは変更しない。保存は `元の名前.edited.pdf` という別名で行う。
- パスワード保護されたPDFでは、まず空パスワードでの読み込みが自動的に試される。それで開けない場合のみパスワード入力欄が表示される。

## 動作確認

確認は「どの環境で」「どのPDFで」行ったかまで含めて記録する。未確認の項目を確認済みと書かない。

### v0.4.3 で確認済み

静的HTTPサーバー配信 + Chromium（Playwrightによる自動操作）での確認。対象は公開PDF `22550.pdf`（糸満市が公開している資料。リポジトリへは含めていない）。engine v0.4.2 単体の検証では `FALLBACK_FONT_METRICS_UNAVAILABLE` で失敗していた「令和 → しょ」（`/DescendantFonts` がinline dictionaryのCIDFont）が、v0.4.3 の取り込みにより**本ツールのUIから初めて成立した。**

- [x] `22550.pdf` を選択し、8,595件のテキストを認識する（「Engine」欄に v0.4.3 表示）
- [x] 「令和」を検索し、34件の検索結果が得られる
- [x] 先頭の検索結果「令和8年度…」を選ぶ
- [x] 置換文字へ「しょ」を入力し、置換が成功する（`checkTextMatchReplacement()` の事前確認で `allowed: true`、mode `fallback-font-multi-run`）
- [x] 編集中プレビューが自動的に表示へ切り替わり、変更内容に「「令和」→「しょ」」が記録される（変更 1件）
- [x] 保存後、本ツールで再open して「しょ」を検索でき、1件見つかる
- [x] 再open後に「令和」を検索すると34件から33件へ減っている（対象の「令和」が消えている）
- [x] **後続の「8年度」の描画位置が、置換前後で完全に一致する**（このツールとは独立したPDF実装 pdfminer.six でグリフ座標を比較し、差 0.000pt）
- [x] 保存したPDFの構造をqpdfで検証し、エラーが無い
- [x] 保存したPDFを、Chromium本体のPDFビューア（本ツールのiframeとは別の、独立したページ表示）で開き、「しょ 8 年度」の表示を目視確認
- [x] 別インスタンス（別ブラウザcontext）で同じ `22550.pdf` を開き直し、「令和 → 平成」（元fontで書ける既存経路、mode `same-length`）も引き続き成功する。保存・再open後、「平成」が検索でき、「令和」は33件に減っている
- [x] 実行中、同一origin以外への要求が発生しない

既存機能の回帰も、同じ `22550.pdf` を使って確認した（NGなし）。

- [x] 1文字検索（「度」）・複数文字検索（「令和」）
- [x] 空欄への置換による削除（mode `delete`）
- [x] 元のフォントに無い文字を含む異文字数置換（「令和」→「しょうわ」、mode `fallback-font-multi-run`）
- [x] 変更前／編集中プレビューの切り替えと、変更履歴への複数件の記録
- [x] 処理中（busy）は検索欄・置換欄・PDF選択が無効化される（二重実行防止）
- [x] Blob URLの作成・解放が設計どおりの対応関係で行われ、増え続けない（`URL.createObjectURL` / `revokeObjectURL` の呼び出し回数を独立に計測して確認）
- [x] 編集用フォントを配信できない場合（HTTP 404）、起動時にエラーを表示してPDFを受け付けない
- [x] PDF以外のファイルで「PDFファイルではありません」と案内する

**この確認は、対応可能な実PDFの範囲が広がったことの記録であり、「任意のPDFを編集できる」ことや「一般職員向けに運用可能」であることを意味しない。運用可否は引き続き未判定である。** 詳細は `docs/feasibility.md` の該当節を参照する。

### v0.4.2 で確認済み

静的HTTPサーバー配信 + Chromium（Playwrightによる自動操作）での確認。対象は開発環境で生成した日本語PDFで、IPAGothicのサブセットを埋め込み、「し」「ょ」「う」「わ」は本文にも埋め込みフォントのサブセットにも含まれていない。本文には、engineから見て描き方の異なる行（`Tj` / `TJ` / `'`、文字間隔の指定がある行、語句が複数の描画単位に分かれた行）を混在させた。あわせて、フォントの幅情報がindirect objectで書かれたPDF、AES-256 / R6 の暗号化PDF、`/P` で変更を禁止したPDFも用いた。**利用者環境の実業務PDFでの確認は含まない（「未確認」を参照）。**

従来経路。

- [x] `vendor/idontlovepdf-engine.js` がES Moduleとして読み込める
- [x] 「Engine」欄に、同梱したengine version（v0.4.2）が表示される
- [x] 通常PDFで本文を認識し、件数が表示される
- [x] PDF選択・ドラッグ＆ドロップの両方で読み込める
- [x] 2文字以上の日本語検索ができる
- [x] 複数の描画単位に分かれた語句を、続いた1つの文字列として検索できる
- [x] 検索結果から1件を選べる。選ぶまで「置換」は押せない
- [x] 1つの単位に収まっている語句を、同文字数・異文字数のどちらへも置き換えられる
- [x] 複数の描画単位に分かれた語句を置き換えられる
- [x] 空欄への置換で文字を削除できる
- [x] 変更前PDF・編集中PDFをプレビューでき、置換後は編集中プレビューが変更前とは別のPDFへ切り替わる
- [x] プレビューの「変更前」「編集中」を何度でも切り替えられる
- [x] 成功した置換が変更履歴に残り、変更件数が増える
- [x] 編集済みPDFを `元の名前.edited.pdf` として保存できる
- [x] 保存後、本ツールでそのPDFを開き直し、置換後の文字を検索できる
- [x] 置換した箇所に元の文字が残っていない
- [x] 保存したPDFを、このツールとは独立したPDF実装（pdfminer.six / pikepdf）で読み取れる
- [x] 保存したPDFをブラウザ標準のPDFビューアで表示できる（置換箇所を目視確認）
- [x] パスワード必須PDF（AES-256 / R6）で、誤ったパスワードと正しいパスワードが区別される
- [x] 暗号化PDF・変更禁止PDF（`/P`）で、それぞれ区別した案内が出て、編集状態が壊れない
- [x] 検索欄を書き換えると前の検索結果と選択が無効になる（置換対象のずれ防止）
- [x] PDF以外のファイルでエラー表示になる
- [x] 同一origin以外への要求が1件も発生しない

編集用フォント経路。

- [x] 起動時に編集用フォントを読み込み、以後は再取得しない（`.ttf` の要求は1回だけ）
- [x] PDFを開いた時点で、編集用フォントがengineで実際に読めることを確認してから編集画面を開く
- [x] 元のフォントに無い文字を含む置換（「令和」→「しょ」「しょうわ」）で、置換ボタンが本体側の文字数判定で無効にならない
- [x] engineの事前確認が許可し、置換できる（mode: `fallback-font-partial`）
- [x] `TJ` で描かれた箇所でも、元のフォントに無い文字へ置換できる（同文字数・異文字数の両方）
- [x] **その置換の後、後続の文字の位置が元のPDFから動いていない（独立したPDF実装で座標を比較し、差 0.000 pt）**
- [x] 編集済みPDFを保存し、再openして置換後の文字を検索できる
- [x] 独立したPDF実装、およびブラウザ標準のPDFビューアで置換後の文字が表示される
- [x] 既存フォントだけで置換できる場合はPDFサイズが約3MB増えない（+約0.4KB）
- [x] 同一PDFでfallback置換を繰り返しても、フォントは1回だけ埋め込まれる（2回目以降は+約1.2KB）
- [x] engineが幅情報を正確に読めないフォントでは、`FALLBACK_FONT_METRICS_UNAVAILABLE` で断り、編集状態を変えない（fail closed）
- [x] engineが安全に置換できないと判断した箇所は「この箇所は置き換えられません」と案内し、編集状態を変えない
- [x] 上記の案内にPDF内部用語（CMap、glyph、Tj、TJ、CID、`/W`、`/Widths`、text matrix、writing mode、run、object番号）が出ない
- [x] engineが返す `code` / `reason` / `unsafeReason` は「詳細（開発者向け）」欄にだけ出る
- [x] 編集用フォントを配信できない場合（HTTP 404）、起動時に原因の分かるエラーを表示し、PDFを受け付けない
- [x] TTFでないファイルが200で返る場合、PDFを開いた時点で検出し、編集画面を開かずエラーにして以後PDFを受け付けない

### 未確認

- [ ] GitHub Pages 上での動作
- [ ] IIS等の庁内静的Webサーバー上での動作（`.ttf` の配信を含む）
- [ ] 庁内端末（実運用ブラウザ・フォント構成）での動作
- [ ] 利用者環境の実業務PDFでの、v0.4.3における検索・置換（v0.3.0 までに読み込みのみ確認した `2024_subsidy_koubo_outline.pdf` を含む）
- [ ] 複合機・Adobe製品など、`22550.pdf` 以外の生成元の実業務PDFでの編集品質・成功率

未確認の項目は、確認できた時点でこのチェックリストと `docs/feasibility.md` を更新する。

## エンジン・編集用フォントの更新

`vendor/README.md` の更新手順を参照する。要点は次のとおり。

1. `idontlovepdf-engine` で新versionをRelease
2. Release asset `idontlovepdf-engine.js` を取得し、`.sha256` と照合
3. `vendor/idontlovepdf-engine.js` を差し替え
4. `ENGINE_VERSION` の表示を確認
5. `idontlovepdf` 側で実ブラウザ回帰確認

engine versionと編集用フォントのversion・SHA-256は `vendor/README.md` で管理する。JavaScriptコードへ版数を書き写さない。実行時のversion表示にはbundleがexportする `ENGINE_VERSION` を使う。

編集用フォントは、engine側の開発・テストで使われているものと同じversionを保つ。勝手に別のフォント・別のversionへ差し替えない。

## 既知の制限

- GitHub Pages / IIS 上での動作は未確認（「動作確認」を参照）。
- PDFの作りによっては、元のフォントに無い文字への置換や、文字数が変わる置換を断られる箇所がある。engineが安全に書き換えられないと判断した場合で、本ツールでは迂回しない（「置き換えられない場合」を参照）。
- 編集用フォントが使われた場合、保存するPDFのサイズが約3MB増える。
- 編集用フォントを使った置換では、後続の文字の位置を動かさないことを優先する。そのため置換後の文字列が元より長い（幅を取る）場合、後続の文字と重なって見えることがある。
- 置換は1件ずつ行う。「すべて置換」は用意していない。
- 検索結果の表示は先頭100件まで。件数は全件を表示するが、それ以降を選ぶには検索する文字を長くして絞り込む。
- プレビューは確認用である。プレビュー上の文字を直接編集したり、置換した箇所へ自動でスクロールしたりはしない。
- 暗号化PDFは検索まで。編集結果の保存はengine側で未対応。
- `/P` permission（変更の可否）は、置換を試みた時点で判明する。読み込んだ時点では表示しない。
- スキャンPDF（画像のみのPDF）は本文テキストを持たないため対象外。

## バージョン

| 区分 | 値 |
|---|---|
| 本ツール | v0.4.3 |
| 同梱engine | idontlovepdf-engine v0.4.3 |
| 同梱編集用フォント | BIZ UDGothic Regular 1.05 |

v0.4.0 から、本ツールのversionを同梱engineのversionへ合わせる。

## ドキュメント

| ファイル | 役割 |
|---|---|
| `docs/feasibility.md` | 実現可能性の検討経緯と技術判断の記録 |
| `vendor/README.md` | 同梱engine・編集用フォントの情報と更新手順 |
| `reference/guide_context.md` | 開発ガイド `lg_toolkit_guide` の最小ガイド（参照用） |

過去の判断経緯は `docs/feasibility.md` に委ねる。本READMEは現在仕様を示すものとし、履歴を積み上げない。
